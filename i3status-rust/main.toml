[theme]
theme = "native"

[icons]
icons = "material-nf"

[[block]]
block = "custom"
json = true
command = "sway-idle-watch"
interval = 1
[[block.click]]
button = "left"
cmd = "sway-idle-toggle"
[block.theme_overrides]
idle_fg = "#aeb3bb"  # comment color
info_fg = "#bf616a"  # red

[[block]]
block = "custom"
command = "ff-volume-watch"
#interval = 0.5  # restarting in script
[[block.click]]
button = "left"
cmd = "ff-volume-toggle"

[[block]]
block = "music"
player = ["spotify", "audacious"]
format = '   {$combo.str(max_w:120, rot_interval:0.5) $prev $play $next |}'
[[block.click]]
button = "up"
cmd = "playerctl previous"
[[block.click]]
button = "down"
cmd = "playerctl next"
[block.theme_overrides]
info_fg = "#8fbcbb"  # cyan

[[block]]
block = "time"
interval = 0.25
format = "   $timestamp.datetime(f:'%a %d %b %Y')   $timestamp.datetime(f:'%H:%M:%S') "
