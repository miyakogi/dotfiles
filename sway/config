##################################################
# Configuration File for Sway Wayland Compositor #
##################################################

### Load sway configurations
# import files from user config
include config.d/*.conf

### Import other files from system
# There only 50-systemd-user.conf is placed, but it's included into local config now
#include /etc/sway/config.d/*

# vim: shiftwidth=4
