# Ghostty Config File

font-family = "Maple Mono"
font-size = 16.5
# font-feature = -dlig  # disabled by default now

font-variation = "wght=300"
font-variation-bold = "wght=600"
font-variation-italic = "wght=300"
font-variation-bold-italic = "wght=600"

# Alternate italic `k` on maple mono
font-feature = +cv34
# Alternate italic `l` on maple mono
font-feature = +cv35
# Alternate left and right arrow `>= / <=`on maple mono
font-feature = +cv64
# arbitrary tagging on maple mono
font-feature = +ss03

# theme = iceberg-dark
# theme = nightfox
# theme = PaleNightHC
# theme = Peppermint
# theme = Raycast_Dark
# theme = Snazzy
theme = carbonfox-vivid-oled
background-opacity = 0.80

command = fish

shell-integration = fish
shell-integration-features = cursor,sudo,no-title

cursor-style = block
cursor-style-blink = false
cursor-opacity = 0.9

custom-shader = shaders/cursor_frozen.glsl

window-decoration = false
window-padding-x = 12
window-padding-y = 12

quit-after-last-window-closed = false

# vim: ft=ini
